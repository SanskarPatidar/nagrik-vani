name: Build and Deploy Config Server

on:
  push:
    branches: [ "main" ]
    paths:
      - 'config-server/**'
      - 'pom.xml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Create Maven settings.xml
        run: echo "${{ secrets.MAVEN_SETTINGS_XML }}" > settings.xml

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./config-server/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/config-server:latest 
            ${{ secrets.DOCKERHUB_USERNAME }}/config-server:${{ github.sha }}
            ${{ secrets.DOCKERHUB_USERNAME }}/config-server:0.0.1-SNAPSHOT
          build-args: |
            MODULE_DIR=config-server
          secrets: |
            maven-settings=settings.xml
          cache-from: type=gha
          cache-to: type=gha,mode=max
